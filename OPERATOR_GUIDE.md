# OYES 실무자 사용 설명서

이 문서는 `오예스 아침 운동` 출결 시스템을 실제 학교 현장에서 운영하는 교사와 담당자를 위한 실무용 안내서입니다.

대상 사용자:

- 현장 교사: 체크인/체크아웃 스캔 담당
- 운영 관리자: 학생 명단 업로드, QR 출력, 예외 처리, 로그 확인 담당

기본 화면:

- 대시보드: `/`
- 체크인 스캔: `/teacher/check-in`
- 체크아웃 스캔: `/teacher/check-out`
- 수동 처리: `/admin/manual`
- 학생 명단 업로드: `/admin/students`
- QR 인쇄/재발급: `/admin/qr`
- 운영 로그: `/admin/logs`

## 1. 운영 전 준비

행사 전에 아래 4가지를 먼저 끝내야 합니다.

1. 학생 명단 업로드
2. 학생 QR 출력 및 배부
3. 교사용 기기 접속 확인
4. 관리자와 교사 로그인 정보 확인

### 1-1. 학생 명단 업로드

관리자 화면 `/admin/students` 에서 진행합니다.

준비물:

- 학교 학급 명단 CSV 파일
- 또는 엑셀에서 CSV로 저장한 파일

필수 헤더:

- `학번`
- `이름` 또는 `성명`
- `성별`

선택 헤더:

- `사진URL`

작업 순서:

1. `/admin/students` 접속
2. `CSV 템플릿 다운로드`를 눌러 형식 확인
3. `CSV 파일` 업로드 또는 `CSV 텍스트 붙여넣기`
4. `명단 업로드` 클릭
5. 업로드 결과에서 `생성`, `갱신`, `재활성화`, `건너뜀` 숫자 확인

주의:

- 학번이 중복되면 일부 행은 자동으로 건너뜁니다.
- `학년`, `반`은 CSV에 직접 넣지 않습니다. 학번에서 자동 계산됩니다.
- 허용 학번 형식은 4자리 또는 5자리입니다.
- 명단 수정이 필요하면 같은 화면에서 다시 업로드하면 됩니다.

### 1-2. QR 출력 및 배부

관리자 화면 `/admin/qr` 에서 진행합니다.

작업 순서:

1. `/admin/qr` 접속
2. 필요하면 `검색`, `반` 필터 입력
3. `인쇄 대상 필터` 클릭
4. 우측 상단 `인쇄` 버튼 클릭
5. 학생별 QR 종이를 배부

알아둘 점:

- QR은 학생마다 고유합니다.
- QR 재발급 시 기존 종이는 즉시 무효가 됩니다.
- QR이 훼손되거나 분실되면 해당 학생 카드에서 `QR 재발급` 버튼을 사용합니다.

### 1-3. 교사용 기기 준비

교사 화면은 두 종류입니다.

- 체크인 담당: `/teacher/check-in`
- 체크아웃 담당: `/teacher/check-out`

권장 준비:

- 휴대폰 또는 태블릿 1대 이상
- 가능한 경우 USB 스캐너 또는 안정적인 카메라
- 배터리 50% 이상
- 와이파이 또는 안정적인 모바일 데이터

운영 규칙:

- 체크인 화면은 입구 기기에서만 사용
- 체크아웃 화면은 출구 기기에서만 사용
- 기기와 담당 교사를 바꾸면 서버에서 차단될 수 있음

### 1-4. 로그인

이 시스템은 Basic Auth 로그인 창이 먼저 뜹니다.

역할별 사용 범위:

- 교사 계정: `/teacher/*`, `/api/scan/*`
- 관리자 계정: 전체 운영 화면

실무자는 배포받은 학교용 로그인 정보만 사용하면 됩니다. `.env` 값은 운영 담당자만 관리합니다.

## 2. 행사 당일 운영

## 2-1. 시작 전 점검

행사 시작 10분 전까지 아래 항목을 확인합니다.

1. 대시보드 `/` 에서 오늘 행사 시간이 맞는지 확인
2. 체크인 담당 기기에서 `/teacher/check-in` 열기
3. 체크아웃 담당 기기에서 `/teacher/check-out` 열기
4. 카메라 권한 허용 또는 스캐너 포커스 확인
5. 학생 QR 지참 여부 마지막 안내

### 2-2. 체크인 운영

체크인 담당 교사는 `/teacher/check-in` 화면을 사용합니다.

화면에서 보이는 항목:

- 행사 허용 시간
- 기기 ID
- 운영자 이메일
- `카메라로 스캔하기`
- 토큰 입력칸
- 최근 체크인 로그
- `대기 스캔 재전송`

운영 방법:

1. 학생이 QR을 보여주면 카메라 또는 스캐너로 읽기
2. 성공 시 `체크인 완료` 메시지 확인
3. 실패 시 화면 메시지를 보고 바로 분기 처리

실패 메시지 해석:

- `등록되지 않은 학생 또는 QR 토큰입니다.`
  명단 미등록 또는 잘못된 QR입니다.
- `체크인 가능 시간이 아닙니다.`
  허용 시간 밖입니다.
- `이미 체크인된 학생입니다.`
  중복 스캔입니다.
- `오프라인 상태라 로컬 큐에 저장했습니다.`
  네트워크가 끊겼지만 나중에 재전송 가능합니다.

### 2-3. 체크아웃 운영

체크아웃 담당 교사는 `/teacher/check-out` 화면을 사용합니다.

운영 방식은 체크인과 동일합니다. 단, 허용 시간은 `8:20 이후` 기준 행사 설정값을 따릅니다.

실패 메시지 해석:

- `체크아웃 가능 시간이 아닙니다.`
- `이미 체크아웃된 학생입니다.`
- `등록되지 않은 학생 또는 QR 토큰입니다.`

### 2-4. 오프라인 또는 서버 불안정 시

이 시스템은 교사 화면에서 네트워크 장애를 대비해 로컬 재전송 큐를 사용합니다.

동작 방식:

- 오프라인이면 스캔 요청을 브라우저에 임시 저장
- 서버 오류가 나도 일부 요청은 큐에 저장
- 연결이 복구되면 `대기 스캔 재전송` 버튼으로 다시 전송 가능

실무자 행동 요령:

1. 화면에 `로컬 큐에 저장했습니다` 메시지가 나오면 학생을 세워두지 말고 계속 진행
2. 상단의 `대기 재전송 N건` 숫자 확인
3. 연결이 안정되면 `대기 스캔 재전송` 클릭
4. 재전송 후 최근 로그와 대시보드 반영 여부 확인

중요:

- 같은 기기, 같은 브라우저에서 재전송해야 큐가 남아 있습니다.
- 브라우저 데이터를 지우면 대기 큐도 사라질 수 있습니다.

## 3. 예외 처리

### 3-1. 학생이 QR을 못 보여주는 경우

관리자 화면 `/admin/manual` 에서 수동 처리합니다.

사용 예:

- 휴대폰 배터리 방전
- QR 종이 훼손
- 학생이 기기를 두고 옴

작업 순서:

1. `/admin/manual` 접속
2. `학번 또는 QR 토큰` 입력
3. `운영자 이메일`, `기기 ID` 확인
4. `사유` 입력
5. `수동 체크인` 또는 `수동 체크아웃` 클릭

원칙:

- 수동 처리는 관리자만 사용
- 사유를 짧게라도 반드시 남김
- 학생이 몰리는 시간에는 현장 흐름을 막지 않도록 최소한으로 사용

### 3-2. QR 재발급이 필요한 경우

`/admin/qr` 에서 해당 학생 카드의 `QR 재발급` 버튼을 사용합니다.

이때 발생하는 변화:

- 기존 QR 즉시 무효화
- 새 버전 QR 발급
- 발급 시각과 처리자 기록 저장

따라서 재발급 후에는 반드시 새 종이나 새 이미지를 학생에게 다시 전달해야 합니다.

## 4. 운영 중 확인할 화면

### 4-1. 대시보드 `/`

이 화면에서는 아래 내용을 확인합니다.

- 대상 학생 수
- 체크인 완료 수
- 체크아웃 완료 수
- 최종 완료 수
- 체크인 누락 계열
- 체크아웃 누락

활용 방법:

- 행사 진행률을 실시간으로 확인
- 특정 반이나 학생 검색
- 상태별 필터로 미처리 학생 추적

### 4-2. 운영 로그 `/admin/logs`

이 화면에서는 아래 내용을 확인합니다.

- 체크인/체크아웃 전체 로그
- 유효/무효 구분
- 수동 처리 이력
- QR 발급/재발급 이력
- 운영 로그 CSV 다운로드

활용 방법:

- 문제 학생 추적
- 왜 무효 처리됐는지 확인
- 행사 종료 후 보고용 자료 추출

## 5. 행사 종료 후 마감

행사 종료 후 관리자 기준 마감 순서입니다.

1. 대시보드에서 `최종 완료`, `누락` 수치 확인
2. `/admin/logs` 에서 무효 스캔과 수동 처리 내역 확인
3. 필요한 경우 `운영 로그 CSV` 다운로드
4. QR 재발급 학생이 있다면 재배부 여부 확인
5. 다음 행사 전까지 기기 충전 및 보관

## 6. 자주 발생하는 문제

### Q1. 학생 QR을 찍었는데 반응이 없습니다.

확인 항목:

- 카메라 권한 허용 여부
- 입력칸 포커스 여부
- QR이 너무 구겨지지 않았는지
- 화면 밝기가 너무 낮지 않은지

### Q2. 중복 처리 메시지가 뜹니다.

이미 처리된 학생입니다. 대시보드 또는 최근 로그에서 해당 학생의 상태를 먼저 확인합니다.

### Q3. 시간 밖이라 등록이 안 됩니다.

행사 시간 설정에 따라 자동 차단된 것입니다. 정말 예외가 맞으면 관리자 화면에서 수동 처리합니다.

### Q4. 와이파이가 불안정합니다.

교사 화면의 로컬 큐 기능을 사용합니다. 대기 수가 쌓여도 브라우저를 닫지 말고, 연결 복구 후 재전송합니다.

### Q5. 잘못된 기기에서 접속했다는 식으로 동작하지 않습니다.

운영자와 기기 조합이 맞지 않을 수 있습니다. 체크인 담당 기기는 체크인용, 체크아웃 담당 기기는 체크아웃용으로 사용합니다.

## 7. 권장 운영 역할 분담

최소 인원 기준:

- 체크인 담당 1명
- 체크아웃 담당 1명
- 관리자 1명

학생 수가 150명 이상이면 권장:

- 체크인 담당 2명
- 체크아웃 담당 2명
- 관리자 1명
- 예외 처리 보조 1명

## 8. 실무자용 하루 체크리스트

### 행사 전날

- 학생 명단 최신본 업로드
- QR 출력 및 재발급 대상 확인
- 교사용 기기 충전
- 로그인 정보 재확인

### 행사 당일 시작 전

- 대시보드 행사 시간 확인
- 교사 화면 열기
- 카메라/스캐너 점검
- 네트워크 상태 확인

### 행사 종료 후

- 누락 학생 확인
- 수동 처리 기록 확인
- 운영 로그 CSV 저장
- 기기 회수

## 9. 운영 담당자 메모

기술 운영 담당자가 함께 보면 좋은 문서:

- 프로젝트 개요: [`README.md`](./README.md)
- 진행 기록: [`oyes-web-attendance_20260422_144518.md`](./oyes-web-attendance_20260422_144518.md)
- 세션 4 안정화: [`SESSION4_PROGRESS.md`](./SESSION4_PROGRESS.md)

이 문서는 화면 이름과 동작 기준으로 작성되어 있으므로, 버튼 문구가 바뀌면 함께 업데이트해야 합니다.
